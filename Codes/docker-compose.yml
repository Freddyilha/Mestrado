services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: rust-cpp-dev
    volumes:
      - ./workspace:/workspace:Z
    working_dir: /workspace
    stdin_open: true
    tty: true
    restart: unless-stopped
